# GitLab initiales Root-Passwort (auto-generiert beim ersten Start)
# Login: root / dieses Passwort
# Nach erstem Login sofort ändern: User-Icon → Edit Profile → Password
GITLAB_ROOT_PASSWORD=CHANGE_ME_STRONG_PASSWORD
